apps:
- id: prometheus-grafana
  name: 'Monitoring: Prometheus & Grafana'
cluster:
  cloud:
    domain: shalb.net
    provider: digitalocean
    region: do-2
    vpc: myId
  installed: true
  name: test
  provisioner:
    instanceType: n10-large
    type: clusterapi
